<h1>CONCERTS</h1>

<table class='table table-striped table-condensed'>
  <thead>
    <tr>
      <th>Date</th>
      <th>Titre</th>
      <th>Lieu</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @concerts.each do |concert| %>
      <tr>
        <td><%= concert.date %></td>
        <td><%= concert.title %></td>
        <td><%= concert.place %></td>
        <td><a <%= link_to 'Détails', concert, class:"btn btn-default" %></a></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Admin', indexadmin_concerts_path, class:"btn btn-default" %>
